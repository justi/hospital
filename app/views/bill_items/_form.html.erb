<%= form_with(model: bill_item) do |form| %>
  <% if bill_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bill_item.errors.count, "error") %> prohibited this bill_item from being saved:</h2>

      <ul>
      <% bill_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
